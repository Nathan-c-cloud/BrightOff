meta {
  name: Health Check
  type: http
  seq: 1
}

get {
  url: http://localhost:8000/health
  body: none
  auth: none
}

tests {
  test("status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
